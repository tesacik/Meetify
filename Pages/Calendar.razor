@page "/calendar"
@using Microsoft.AspNetCore.Identity
@attribute [Authorize]
@inject UserManager<IdentityUser> UserManager


<h3>Můj kalendář</h3>


@if (_userId is not null)
{
<CalendarMonth Month="_month" OwnerUserId="_userId" IsPublicView="false" />
}
